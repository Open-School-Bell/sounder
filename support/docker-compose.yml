services:
  sounder:
    image: openschoolbell/sounder:2
    restart: always
    devices:
      - /dev/snd:/dev/snd
    ports:
      - 3000:3000
    environment:
      CONTROLLER_URL: <CONTROLLER_ADDRESS>
      ENROLLMENT_KEY: <SOUNDER_KEY>
